import './Timer.svelte.css.proxy.js';
/* src/components/Timer.svelte generated by Svelte v3.32.1 */
import { create_ssr_component, escape } from "../_snowpack/pkg/svelte/internal.js";

import { onMount } from "../_snowpack/pkg/svelte.js";

const css = {
	code: "p.svelte-86eo75{@apply font-mono;}",
	map: "{\"version\":3,\"file\":\"Timer.svelte\",\"sources\":[\"Timer.svelte\"],\"sourcesContent\":[\"<script>\\n  import { onMount } from 'svelte';\\n\\n  let timer = 0;\\n  let seconds;\\n  let minutes;\\n\\n  onMount(() => count());\\n\\n  const count = () => {\\n    timer++;\\n    setTimeout(count, 1000);\\n  };\\n\\n  const pad = (val) => (val > 9 ? val : '0' + val);\\n  $: {\\n    seconds = pad(timer % 60);\\n    minutes = pad(Math.floor(timer / 60));\\n  }\\n</script>\\n\\n<p>You open this page for: {minutes} minutes {seconds} seconds.</p>\\n\\n<style>\\n  p {\\n    @apply font-mono;\\n  }\\n</style>\\n\"],\"names\":[],\"mappings\":\"AAwBE,CAAC,cAAC,CAAC,AACD,OAAO,SAAS,CAAC,AACnB,CAAC\"}"
};

const Timer = create_ssr_component(($$result, $$props, $$bindings, slots) => {
	let timer = 0;
	let seconds;
	let minutes;
	onMount(() => count());

	const count = () => {
		timer++;
		setTimeout(count, 1000);
	};

	const pad = val => val > 9 ? val : "0" + val;
	$$result.css.add(css);

	$: {
		{
			seconds = pad(timer % 60);
			minutes = pad(Math.floor(timer / 60));
		}
	}

	return `<p class="${"svelte-86eo75"}">You open this page for: ${escape(minutes)} minutes ${escape(seconds)} seconds.</p>`;
});

export default Timer;
//# sourceMappingURL=Timer.svelte.js.map
