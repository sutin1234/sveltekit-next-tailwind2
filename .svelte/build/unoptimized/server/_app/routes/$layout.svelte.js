/* src/routes/$layout.svelte generated by Svelte v3.32.1 */
import { create_ssr_component, subscribe } from "../../_snowpack/pkg/svelte/internal.js";

import "./_global.css.proxy.js";
import { theme } from "./store.js";
import { onMount } from "../../_snowpack/pkg/svelte.js";

const $layout = create_ssr_component(($$result, $$props, $$bindings, slots) => {
	let $theme, $$unsubscribe_theme;
	$$unsubscribe_theme = subscribe(theme, value => $theme = value);

	onMount(() => {
		if ($theme === "dark") {
			document.querySelector("html").classList.add("dark");
		}
	});

	$$unsubscribe_theme();
	return `<main>${slots.default ? slots.default({}) : ``}</main>`;
});

export default $layout;
//# sourceMappingURL=$layout.svelte.js.map
