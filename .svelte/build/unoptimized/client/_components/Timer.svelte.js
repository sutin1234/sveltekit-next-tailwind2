import './Timer.svelte.css.proxy.js';
/* src/components/Timer.svelte generated by Svelte v3.32.1 */
import {
	SvelteComponent,
	append,
	attr,
	children,
	claim_element,
	claim_text,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	set_data,
	text
} from "../_snowpack/pkg/svelte/internal.js";

import { onMount } from "../_snowpack/pkg/svelte.js";

function create_fragment(ctx) {
	let p;
	let t0;
	let t1;
	let t2;
	let t3;
	let t4;

	return {
		c() {
			p = element("p");
			t0 = text("You open this page for: ");
			t1 = text(/*minutes*/ ctx[1]);
			t2 = text(" minutes ");
			t3 = text(/*seconds*/ ctx[0]);
			t4 = text(" seconds.");
			this.h();
		},
		l(nodes) {
			p = claim_element(nodes, "P", { class: true });
			var p_nodes = children(p);
			t0 = claim_text(p_nodes, "You open this page for: ");
			t1 = claim_text(p_nodes, /*minutes*/ ctx[1]);
			t2 = claim_text(p_nodes, " minutes ");
			t3 = claim_text(p_nodes, /*seconds*/ ctx[0]);
			t4 = claim_text(p_nodes, " seconds.");
			p_nodes.forEach(detach);
			this.h();
		},
		h() {
			attr(p, "class", "svelte-86eo75");
		},
		m(target, anchor) {
			insert(target, p, anchor);
			append(p, t0);
			append(p, t1);
			append(p, t2);
			append(p, t3);
			append(p, t4);
		},
		p(ctx, [dirty]) {
			if (dirty & /*minutes*/ 2) set_data(t1, /*minutes*/ ctx[1]);
			if (dirty & /*seconds*/ 1) set_data(t3, /*seconds*/ ctx[0]);
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(p);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let timer = 0;
	let seconds;
	let minutes;
	onMount(() => count());

	const count = () => {
		$$invalidate(2, timer++, timer);
		setTimeout(count, 1000);
	};

	const pad = val => val > 9 ? val : "0" + val;

	$$self.$$.update = () => {
		if ($$self.$$.dirty & /*timer*/ 4) {
			$: {
				$$invalidate(0, seconds = pad(timer % 60));
				$$invalidate(1, minutes = pad(Math.floor(timer / 60)));
			}
		}
	};

	return [seconds, minutes, timer];
}

class Timer extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default Timer;
//# sourceMappingURL=Timer.svelte.js.map
