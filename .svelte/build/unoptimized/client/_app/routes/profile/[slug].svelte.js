/* src/routes/profile/[slug].svelte generated by Svelte v3.32.1 */
import {
	SvelteComponent,
	claim_element,
	claim_space,
	claim_text,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	set_data,
	space,
	text
} from "../../../_snowpack/pkg/svelte/internal.js";

function create_fragment(ctx) {
	let t0;
	let t1;
	let t2;
	let br;

	return {
		c() {
			t0 = text("params: ");
			t1 = text(/*slug*/ ctx[0]);
			t2 = space();
			br = element("br");
		},
		l(nodes) {
			t0 = claim_text(nodes, "params: ");
			t1 = claim_text(nodes, /*slug*/ ctx[0]);
			t2 = claim_space(nodes);
			br = claim_element(nodes, "BR", {});
		},
		m(target, anchor) {
			insert(target, t0, anchor);
			insert(target, t1, anchor);
			insert(target, t2, anchor);
			insert(target, br, anchor);
		},
		p(ctx, [dirty]) {
			if (dirty & /*slug*/ 1) set_data(t1, /*slug*/ ctx[0]);
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(t0);
			if (detaching) detach(t1);
			if (detaching) detach(t2);
			if (detaching) detach(br);
		}
	};
}

async function load({ page }) {
	return { props: { slug: page.params.slug } };
}

function instance($$self, $$props, $$invalidate) {
	let { slug } = $$props;

	$$self.$$set = $$props => {
		if ("slug" in $$props) $$invalidate(0, slug = $$props.slug);
	};

	return [slug];
}

class U5Bslugu5D extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { slug: 0 });
	}
}

export default U5Bslugu5D;
export { load };
//# sourceMappingURL=[slug].svelte.js.map
