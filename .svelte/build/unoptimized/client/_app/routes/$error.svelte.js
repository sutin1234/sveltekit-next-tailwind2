/* src/routes/$error.svelte generated by Svelte v3.32.1 */
import {
	SvelteComponent,
	append,
	children,
	claim_element,
	claim_space,
	claim_text,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	set_data,
	space,
	text
} from "../../_snowpack/pkg/svelte/internal.js";

import { onMount } from "../../_snowpack/pkg/svelte.js";

function create_fragment(ctx) {
	let h20;
	let t0;
	let t1;
	let t2;
	let h21;
	let t3;
	let t4;

	return {
		c() {
			h20 = element("h2");
			t0 = text("Error: ");
			t1 = text(/*error*/ ctx[0]);
			t2 = space();
			h21 = element("h2");
			t3 = text("Status: ");
			t4 = text(/*status*/ ctx[1]);
		},
		l(nodes) {
			h20 = claim_element(nodes, "H2", {});
			var h20_nodes = children(h20);
			t0 = claim_text(h20_nodes, "Error: ");
			t1 = claim_text(h20_nodes, /*error*/ ctx[0]);
			h20_nodes.forEach(detach);
			t2 = claim_space(nodes);
			h21 = claim_element(nodes, "H2", {});
			var h21_nodes = children(h21);
			t3 = claim_text(h21_nodes, "Status: ");
			t4 = claim_text(h21_nodes, /*status*/ ctx[1]);
			h21_nodes.forEach(detach);
		},
		m(target, anchor) {
			insert(target, h20, anchor);
			append(h20, t0);
			append(h20, t1);
			insert(target, t2, anchor);
			insert(target, h21, anchor);
			append(h21, t3);
			append(h21, t4);
		},
		p(ctx, [dirty]) {
			if (dirty & /*error*/ 1) set_data(t1, /*error*/ ctx[0]);
			if (dirty & /*status*/ 2) set_data(t4, /*status*/ ctx[1]);
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(h20);
			if (detaching) detach(t2);
			if (detaching) detach(h21);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { error } = $$props, { status } = $$props;

	onMount(() => {
		console.log(error, status);
	});

	$$self.$$set = $$props => {
		if ("error" in $$props) $$invalidate(0, error = $$props.error);
		if ("status" in $$props) $$invalidate(1, status = $$props.status);
	};

	return [error, status];
}

class $error extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { error: 0, status: 1 });
	}
}

export default $error;
//# sourceMappingURL=$error.svelte.js.map
